/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  GetTradesSnapshotParams,
  GetTradesSyncReadParams,
  GetUsersSnapshotParams,
  GetUsersSyncReadParams,
  MT4TradeListApiResponse,
  MT4UserListApiResponse
} from './model';

import { customFetch } from '../mutator';


export type getUsersSnapshotResponse200 = {
  data: MT4UserListApiResponse
  status: 200
}

export type getUsersSnapshotResponseSuccess = (getUsersSnapshotResponse200) & {
  headers: Headers;
};
;

export type getUsersSnapshotResponse = (getUsersSnapshotResponseSuccess)

export const getGetUsersSnapshotUrl = (tradePlatform: string,
    params?: GetUsersSnapshotParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/UsersSnapshot?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/UsersSnapshot`
}

/**
 * Atomic snapshot of all registered users on the MT4 server.
 *
 * Manager (live) call to the wrapper's `UsersSnapshot()`.
 * Iterates `UnpackObject<UserRecord>(i)` over the native
 * array — on mtmanapi64.dll the per-struct cost combined with
 * ASLR alignment triggers access violations after some iterations,
 * so this endpoint is sidecar-only.
 * @summary Snapshot all users
 */
export const getUsersSnapshot = async (tradePlatform: string,
    params?: GetUsersSnapshotParams, options?: RequestInit): Promise<getUsersSnapshotResponse> => {

  return customFetch<getUsersSnapshotResponse>(getGetUsersSnapshotUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getUsersSyncReadResponse200 = {
  data: MT4UserListApiResponse
  status: 200
}

export type getUsersSyncReadResponseSuccess = (getUsersSyncReadResponse200) & {
  headers: Headers;
};
;

export type getUsersSyncReadResponse = (getUsersSyncReadResponseSuccess)

export const getGetUsersSyncReadUrl = (tradePlatform: string,
    params?: GetUsersSyncReadParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/UsersSyncRead?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/UsersSyncRead`
}

/**
 * Read user records buffered by a prior `UsersSyncStart` call.
 *
 * Manager (live) call to the wrapper's `UsersSyncRead()`.
 * Must be preceded by `POST UsersSyncStart` on the main x64
 * API to seed the server-side snapshot. Same UnpackObject hazard
 * as `UsersSnapshot` — sidecar-only.
 * @summary Read user-records sync
 */
export const getUsersSyncRead = async (tradePlatform: string,
    params?: GetUsersSyncReadParams, options?: RequestInit): Promise<getUsersSyncReadResponse> => {

  return customFetch<getUsersSyncReadResponse>(getGetUsersSyncReadUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getTradesSnapshotResponse200 = {
  data: MT4TradeListApiResponse
  status: 200
}

export type getTradesSnapshotResponseSuccess = (getTradesSnapshotResponse200) & {
  headers: Headers;
};
;

export type getTradesSnapshotResponse = (getTradesSnapshotResponseSuccess)

export const getGetTradesSnapshotUrl = (tradePlatform: string,
    params?: GetTradesSnapshotParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/TradesSnapshot?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/TradesSnapshot`
}

/**
 * Atomic snapshot of all open/pending trades on the MT4 server.
 *
 * Manager (live) call to the wrapper's `TradesSnapshot()`.
 * Same UnpackObject-loop hazard as the user batch reads. Sidecar-only.
 * @summary Snapshot all trades
 */
export const getTradesSnapshot = async (tradePlatform: string,
    params?: GetTradesSnapshotParams, options?: RequestInit): Promise<getTradesSnapshotResponse> => {

  return customFetch<getTradesSnapshotResponse>(getGetTradesSnapshotUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getTradesSyncReadResponse200 = {
  data: MT4TradeListApiResponse
  status: 200
}

export type getTradesSyncReadResponseSuccess = (getTradesSyncReadResponse200) & {
  headers: Headers;
};
;

export type getTradesSyncReadResponse = (getTradesSyncReadResponseSuccess)

export const getGetTradesSyncReadUrl = (tradePlatform: string,
    params?: GetTradesSyncReadParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/TradesSyncRead?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/TradesSyncRead`
}

/**
 * Read trade records buffered by a prior `TradesSyncStart` call.
 *
 * Manager (live) call to the wrapper's `TradesSyncRead()`.
 * Must be preceded by `POST TradesSyncStart` on the main x64
 * API. Same UnpackObject hazard — sidecar-only.
 * @summary Read trade-records sync
 */
export const getTradesSyncRead = async (tradePlatform: string,
    params?: GetTradesSyncReadParams, options?: RequestInit): Promise<getTradesSyncReadResponse> => {

  return customFetch<getTradesSyncReadResponse>(getGetTradesSyncReadUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


