/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  BooleanApiResponse,
  GetChartRequestSymbolParams,
  GetTicksRequestSymbolParams,
  Int32ApiResponse,
  MT4ChartBarListApiResponse,
  MT4ChartWriteRequest,
  MT4TickInfoApiResponse,
  MT4TickInfoListApiResponse,
  MT4TickRecordListApiResponse,
  PostChartAddSymbolParams,
  PostChartDeleteSymbolParams,
  PostChartUpdateSymbolParams
} from './model';

import { customFetch } from '../mutator';


export type getChartRequestSymbolResponse200 = {
  data: MT4ChartBarListApiResponse
  status: 200
}

export type getChartRequestSymbolResponseSuccess = (getChartRequestSymbolResponse200) & {
  headers: Headers;
};
;

export type getChartRequestSymbolResponse = (getChartRequestSymbolResponseSuccess)

export const getGetChartRequestSymbolUrl = (tradePlatform: string,
    symbol: string,
    params?: GetChartRequestSymbolParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/ChartRequest/${symbol}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/ChartRequest/${symbol}`
}

/**
 * OHLC chart bars for a symbol over a date range.
 *
 * Manager (live) call. Resolves the symbol's `ConSymbol` first
 * (needed by the wrapper to set scale/digits), then asks for bars of
 * the given `period` in the date window. `mode` defaults to
 * `RangeInExcludeOutOfRage` — bars whose time falls strictly
 * inside the window.
 * @summary Get chart bars
 */
export const getChartRequestSymbol = async (tradePlatform: string,
    symbol: string,
    params?: GetChartRequestSymbolParams, options?: RequestInit): Promise<getChartRequestSymbolResponse> => {

  return customFetch<getChartRequestSymbolResponse>(getGetChartRequestSymbolUrl(tradePlatform,symbol,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type postChartAddSymbolResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postChartAddSymbolResponseSuccess = (postChartAddSymbolResponse200) & {
  headers: Headers;
};
;

export type postChartAddSymbolResponse = (postChartAddSymbolResponseSuccess)

export const getPostChartAddSymbolUrl = (tradePlatform: string,
    symbol: string,
    params?: PostChartAddSymbolParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/ChartAdd/${symbol}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/ChartAdd/${symbol}`
}

/**
 * Append OHLC bars to a symbol's chart history — POST destructive.
 *
 * Manager (live) call that appends the provided bars to the symbol's
 * historical chart for the given `period`. The wrapper looks up the
 * symbol's scale (`Multiply`/`Digits`) internally to encode the
 * float OHLC values back into native integer prices.
 *
 * Idempotency-Key strongly recommended — duplicate writes can corrupt the
 * historical data series.
 * @summary Add chart bars
 */
export const postChartAddSymbol = async (tradePlatform: string,
    symbol: string,
    mT4ChartWriteRequest?: MT4ChartWriteRequest,
    params?: PostChartAddSymbolParams, options?: RequestInit): Promise<postChartAddSymbolResponse> => {

  return customFetch<postChartAddSymbolResponse>(getPostChartAddSymbolUrl(tradePlatform,symbol,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(mT4ChartWriteRequest)
  }
);}


export type postChartUpdateSymbolResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postChartUpdateSymbolResponseSuccess = (postChartUpdateSymbolResponse200) & {
  headers: Headers;
};
;

export type postChartUpdateSymbolResponse = (postChartUpdateSymbolResponseSuccess)

export const getPostChartUpdateSymbolUrl = (tradePlatform: string,
    symbol: string,
    params?: PostChartUpdateSymbolParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/ChartUpdate/${symbol}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/ChartUpdate/${symbol}`
}

/**
 * Overwrite existing OHLC bars in a symbol's chart history — POST destructive.
 *
 * Manager (live) call. Replaces bars at the timestamps provided in
 * `Rates` for the given `period`. Bars whose timestamps don't
 * match an existing bar are silently ignored by the MT4 server.
 *
 * Idempotency-Key strongly recommended.
 * @summary Update chart bars
 */
export const postChartUpdateSymbol = async (tradePlatform: string,
    symbol: string,
    mT4ChartWriteRequest?: MT4ChartWriteRequest,
    params?: PostChartUpdateSymbolParams, options?: RequestInit): Promise<postChartUpdateSymbolResponse> => {

  return customFetch<postChartUpdateSymbolResponse>(getPostChartUpdateSymbolUrl(tradePlatform,symbol,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(mT4ChartWriteRequest)
  }
);}


export type postChartDeleteSymbolResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postChartDeleteSymbolResponseSuccess = (postChartDeleteSymbolResponse200) & {
  headers: Headers;
};
;

export type postChartDeleteSymbolResponse = (postChartDeleteSymbolResponseSuccess)

export const getPostChartDeleteSymbolUrl = (tradePlatform: string,
    symbol: string,
    params?: PostChartDeleteSymbolParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/ChartDelete/${symbol}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/ChartDelete/${symbol}`
}

/**
 * Delete OHLC bars from a symbol's chart history — POST destructive.
 *
 * Manager (live) call. Removes bars whose `Time` matches the
 * entries in `Rates` for the given `period`. Only the bar
 * timestamp is consulted server-side; OHLC values can be zero.
 *
 * Idempotency-Key strongly recommended — silently repeating a delete on
 * already-removed bars is harmless, but accidental double-submit could
 * nudge audit logs with extra "operation requested" entries.
 * @summary Delete chart bars
 */
export const postChartDeleteSymbol = async (tradePlatform: string,
    symbol: string,
    mT4ChartWriteRequest?: MT4ChartWriteRequest,
    params?: PostChartDeleteSymbolParams, options?: RequestInit): Promise<postChartDeleteSymbolResponse> => {

  return customFetch<postChartDeleteSymbolResponse>(getPostChartDeleteSymbolUrl(tradePlatform,symbol,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(mT4ChartWriteRequest)
  }
);}


export type postHistoryCorrectSymbolResponse200 = {
  data: Int32ApiResponse
  status: 200
}

export type postHistoryCorrectSymbolResponseSuccess = (postHistoryCorrectSymbolResponse200) & {
  headers: Headers;
};
;

export type postHistoryCorrectSymbolResponse = (postHistoryCorrectSymbolResponseSuccess)

export const getPostHistoryCorrectSymbolUrl = (tradePlatform: string,
    symbol: string,) => {




  return `/api/v2/MT4/${tradePlatform}/HistoryCorrect/${symbol}`
}

/**
 * Recompute and patch internal consistency of a symbol's chart history.
 *
 * Manager (live) call. The MT4 server walks the symbol's full bar
 * history for every period and fixes inconsistencies (gaps, broken
 * OHLC relationships, mismatched aggregates). Returns the count of
 * bars that the server corrected — zero is a valid result (history
 * was already consistent).
 *
 * Requires Administrator rights on the manager account. The operation
 * can take many seconds on long histories; pair with
 * `Idempotency-Key` for retry safety so a TCP retry doesn't
 * kick off a second full sweep.
 * @summary Repair chart history
 */
export const postHistoryCorrectSymbol = async (tradePlatform: string,
    symbol: string, options?: RequestInit): Promise<postHistoryCorrectSymbolResponse> => {

  return customFetch<postHistoryCorrectSymbolResponse>(getPostHistoryCorrectSymbolUrl(tradePlatform,symbol),
  {
    ...options,
    method: 'POST'


  }
);}


export type getTicksRequestSymbolResponse200 = {
  data: MT4TickRecordListApiResponse
  status: 200
}

export type getTicksRequestSymbolResponseSuccess = (getTicksRequestSymbolResponse200) & {
  headers: Headers;
};
;

export type getTicksRequestSymbolResponse = (getTicksRequestSymbolResponseSuccess)

export const getGetTicksRequestSymbolUrl = (tradePlatform: string,
    symbol: string,
    params?: GetTicksRequestSymbolParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/TicksRequest/${symbol}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/TicksRequest/${symbol}`
}

/**
 * Historical ticks for a symbol over a date range.
 *
 * Manager (live) call returning a list of `MT4TickRecord` DTOs.
 * `flags` controls whether raw and/or normalised ticks are
 * included (defaults to `All`). Heavy endpoint — pair with
 * `Idempotency-Key` for retry safety.
 * @summary Get historical ticks
 */
export const getTicksRequestSymbol = async (tradePlatform: string,
    symbol: string,
    params?: GetTicksRequestSymbolParams, options?: RequestInit): Promise<getTicksRequestSymbolResponse> => {

  return customFetch<getTicksRequestSymbolResponse>(getGetTicksRequestSymbolUrl(tradePlatform,symbol,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getTickInfoLastSymbolResponse200 = {
  data: MT4TickInfoApiResponse
  status: 200
}

export type getTickInfoLastSymbolResponseSuccess = (getTickInfoLastSymbolResponse200) & {
  headers: Headers;
};
;

export type getTickInfoLastSymbolResponse = (getTickInfoLastSymbolResponseSuccess)

export const getGetTickInfoLastSymbolUrl = (tradePlatform: string,
    symbol: string,) => {




  return `/api/v2/MT4/${tradePlatform}/TickInfoLast/${symbol}`
}

/**
 * Last known bid/ask tick for a single symbol from the pump cache.
 *
 * Pump-cached read of the last tick. For sub-second updates prefer the
 * SignalR tick stream over polling. Returns NotFound envelope when the
 * pump cache has no tick for the requested symbol (symbol not in the
 * active subscription set, or the platform has never received a tick
 * since startup).
 * @summary Get last tick (cached)
 */
export const getTickInfoLastSymbol = async (tradePlatform: string,
    symbol: string, options?: RequestInit): Promise<getTickInfoLastSymbolResponse> => {

  return customFetch<getTickInfoLastSymbolResponse>(getGetTickInfoLastSymbolUrl(tradePlatform,symbol),
  {
    ...options,
    method: 'GET'


  }
);}


export type getTickInfoLastResponse200 = {
  data: MT4TickInfoListApiResponse
  status: 200
}

export type getTickInfoLastResponseSuccess = (getTickInfoLastResponse200) & {
  headers: Headers;
};
;

export type getTickInfoLastResponse = (getTickInfoLastResponseSuccess)

export const getGetTickInfoLastUrl = (tradePlatform: string,) => {




  return `/api/v2/MT4/${tradePlatform}/TickInfoLast`
}

/**
 * Last known bid/ask tick for ALL symbols in the pump cache.
 *
 * Snapshot of every symbol the pump has received quotes for. Empty list
 * when no quotes have been received yet. For sub-second updates use the
 * SignalR tick stream — this endpoint is intended for one-shot snapshots
 * (warm-up, monitoring dashboards, idempotent cache scenarios).
 * @summary Get last ticks, all symbols (cached)
 */
export const getTickInfoLast = async (tradePlatform: string, options?: RequestInit): Promise<getTickInfoLastResponse> => {

  return customFetch<getTickInfoLastResponse>(getGetTickInfoLastUrl(tradePlatform),
  {
    ...options,
    method: 'GET'


  }
);}


