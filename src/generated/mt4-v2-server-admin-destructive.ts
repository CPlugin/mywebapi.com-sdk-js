/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  BooleanApiResponse,
  PostSrvChartsSyncParams,
  PostSrvFeedsRestartParams,
  PostSrvLiveUpdateStartParams,
  PostSrvRestartParams
} from './model';

import { customFetch } from '../mutator';


export type postSrvRestartResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postSrvRestartResponseSuccess = (postSrvRestartResponse200) & {
  headers: Headers;
};
;

export type postSrvRestartResponse = (postSrvRestartResponseSuccess)

export const getPostSrvRestartUrl = (tradePlatform: string,
    params?: PostSrvRestartParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/SrvRestart?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/SrvRestart`
}

/**
 * Soft-restart of the MT4 server process. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's `SrvRestart()`. The current
 * manager connection is dropped during the restart cycle — clients
 * should expect the next request to surface a fresh login. Audit lines
 * are emitted on both sides of the wrapper call (independently of the
 * HTTP response).
 * <br>
 * The `?confirm=true` guard rejects calls without the literal
 * query string. It is a deliberateness signal, NOT an authorization
 * mechanism — admin rights are still enforced separately.
 * @summary Restart server
 */
export const postSrvRestart = async (tradePlatform: string,
    params?: PostSrvRestartParams, options?: RequestInit): Promise<postSrvRestartResponse> => {

  return customFetch<postSrvRestartResponse>(getPostSrvRestartUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST'


  }
);}


export type postSrvChartsSyncResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postSrvChartsSyncResponseSuccess = (postSrvChartsSyncResponse200) & {
  headers: Headers;
};
;

export type postSrvChartsSyncResponse = (postSrvChartsSyncResponseSuccess)

export const getPostSrvChartsSyncUrl = (tradePlatform: string,
    params?: PostSrvChartsSyncParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/SrvChartsSync?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/SrvChartsSync`
}

/**
 * Force chart-history resync across the plugin set. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's `SrvChartsSync()`. Forces
 * the MT4 server to walk every plugin's chart-history feed and bring
 * the local cache in sync. Idempotent — running twice is a no-op
 * against an already-synced state.
 * @summary Force chart resync
 */
export const postSrvChartsSync = async (tradePlatform: string,
    params?: PostSrvChartsSyncParams, options?: RequestInit): Promise<postSrvChartsSyncResponse> => {

  return customFetch<postSrvChartsSyncResponse>(getPostSrvChartsSyncUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST'


  }
);}


export type postSrvLiveUpdateStartResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postSrvLiveUpdateStartResponseSuccess = (postSrvLiveUpdateStartResponse200) & {
  headers: Headers;
};
;

export type postSrvLiveUpdateStartResponse = (postSrvLiveUpdateStartResponseSuccess)

export const getPostSrvLiveUpdateStartUrl = (tradePlatform: string,
    params?: PostSrvLiveUpdateStartParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/SrvLiveUpdateStart?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/SrvLiveUpdateStart`
}

/**
 * Kick the LiveUpdate distributor. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's `SrvLiveUpdateStart()`.
 * Starts (or restarts) the server's outbound LiveUpdate broadcast.
 * Affects connected client terminals — they may receive an update prompt.
 * @summary Start LiveUpdate distributor
 */
export const postSrvLiveUpdateStart = async (tradePlatform: string,
    params?: PostSrvLiveUpdateStartParams, options?: RequestInit): Promise<postSrvLiveUpdateStartResponse> => {

  return customFetch<postSrvLiveUpdateStartResponse>(getPostSrvLiveUpdateStartUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST'


  }
);}


export type postSrvFeedsRestartResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postSrvFeedsRestartResponseSuccess = (postSrvFeedsRestartResponse200) & {
  headers: Headers;
};
;

export type postSrvFeedsRestartResponse = (postSrvFeedsRestartResponseSuccess)

export const getPostSrvFeedsRestartUrl = (tradePlatform: string,
    params?: PostSrvFeedsRestartParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/SrvFeedsRestart?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/SrvFeedsRestart`
}

/**
 * Restart all running data feeders. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's `SrvFeedsRestart()`.
 * Cycles all running quote/news feeders. May cause a brief gap in
 * the tick stream — typically a second or two. Idempotent.
 * @summary Restart data feeders
 */
export const postSrvFeedsRestart = async (tradePlatform: string,
    params?: PostSrvFeedsRestartParams, options?: RequestInit): Promise<postSrvFeedsRestartResponse> => {

  return customFetch<postSrvFeedsRestartResponse>(getPostSrvFeedsRestartUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST'


  }
);}


