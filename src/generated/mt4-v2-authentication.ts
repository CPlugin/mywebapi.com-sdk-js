/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  BooleanApiResponse,
  PostUserPasswordSetLoginParams
} from './model';

import { customFetch } from '../mutator';


export type postUserPasswordCheckLoginResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postUserPasswordCheckLoginResponseSuccess = (postUserPasswordCheckLoginResponse200) & {
  headers: Headers;
};
;

export type postUserPasswordCheckLoginResponse = (postUserPasswordCheckLoginResponseSuccess)

export const getPostUserPasswordCheckLoginUrl = (tradePlatform: string,
    login: number,) => {




  return `/api/v2/MT4/${tradePlatform}/UserPasswordCheck/${login}`
}

/**
 * Verify an account password against the MT4 server.
 *
 * POST body: the candidate password as a JSON string (e.g. `"secret123"`).
 * Returns envelope with bool payload — `true` if MT4 server accepts
 * the password, `false` when wrapper returns InvalidLoginOrPassword
 * (envelope marked as MT4Error with the underlying ResultCode in
 * managerAPICode).
 *
 * Read-only operation: no state changes. Idempotency-Key on this endpoint
 * is supported but rarely useful — pin if your retry policy expects the
 * same answer across attempts.
 * @summary Verify account password
 */
export const postUserPasswordCheckLogin = async (tradePlatform: string,
    login: number,
    postUserPasswordCheckLoginBody?: string, options?: RequestInit): Promise<postUserPasswordCheckLoginResponse> => {

  return customFetch<postUserPasswordCheckLoginResponse>(getPostUserPasswordCheckLoginUrl(tradePlatform,login),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(postUserPasswordCheckLoginBody)
  }
);}


export type postUserPasswordSetLoginResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postUserPasswordSetLoginResponseSuccess = (postUserPasswordSetLoginResponse200) & {
  headers: Headers;
};
;

export type postUserPasswordSetLoginResponse = (postUserPasswordSetLoginResponseSuccess)

export const getPostUserPasswordSetLoginUrl = (tradePlatform: string,
    login: number,
    params?: PostUserPasswordSetLoginParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/UserPasswordSet/${login}?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/UserPasswordSet/${login}`
}

/**
 * Set the account password — Type 1 mutator (direct overwrite).
 *
 * POST body: the new password as a JSON string. Optional query params:
 * `changeInvestor=true` sets the read-only investor password instead
 * of the primary one; `cleanPubkey=true` resets the public key alongside
 * the password (caller's RSA-protected secondary auth).
 *
 * <br>
 * This is a Type 1 mutator — full-replace semantics. The wrapper accepts
 * the new password directly without a read-modify-write loop. There is no
 * Type 2 ("set only this field, leave the rest alone") variant of password
 * change because the password is itself a single field — the read step
 * would be tautological.
 * <br>
 * Idempotency-Key is strongly recommended. A retried password change without
 * the header risks setting it twice (the second call returns success even
 * though the password is the same), which is harmless but generates audit
 * noise. With the header, the second call short-circuits to the cached
 * response.
 * @summary Set account password
 */
export const postUserPasswordSetLogin = async (tradePlatform: string,
    login: number,
    postUserPasswordSetLoginBody?: string,
    params?: PostUserPasswordSetLoginParams, options?: RequestInit): Promise<postUserPasswordSetLoginResponse> => {

  return customFetch<postUserPasswordSetLoginResponse>(getPostUserPasswordSetLoginUrl(tradePlatform,login,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(postUserPasswordSetLoginBody)
  }
);}


