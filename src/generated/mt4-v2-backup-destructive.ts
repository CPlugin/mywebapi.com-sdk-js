/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  BooleanApiResponse,
  MT4TradeRestoreInput,
  MT4TradeRestoreResultListApiResponse,
  MT4UserRestoreInput,
  PostBackupRestoreOrdersParams,
  PostBackupRestoreUsersParams
} from './model';

import { customFetch } from '../mutator';


export type postBackupRestoreUsersResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postBackupRestoreUsersResponseSuccess = (postBackupRestoreUsersResponse200) & {
  headers: Headers;
};
;

export type postBackupRestoreUsersResponse = (postBackupRestoreUsersResponseSuccess)

export const getPostBackupRestoreUsersUrl = (tradePlatform: string,
    params?: PostBackupRestoreUsersParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/BackupRestoreUsers?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/BackupRestoreUsers`
}

/**
 * Restore user records into the live MT4 database. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's
 * `BackupRestoreUsers(UserRecord[] users)`. Each input
 * `MT4UserRestoreInput` is mapped to a fresh `UserRecord`
 * with the narrow restore field set — secrets, OTP, server-managed
 * timestamps, and reserved blobs are NOT carried (see DTO docs).
 * <br>
 * Idempotency-Key header is <b>strongly recommended</b>: a network
 * blip during a multi-user restore can leave the client uncertain
 * whether the write happened. Without the key, retry will double-write.
 * <br>
 * Batch cap: 10000 records per call. Larger restores must be split.
 * @summary Restore users from backup
 */
export const postBackupRestoreUsers = async (tradePlatform: string,
    mT4UserRestoreInput?: MT4UserRestoreInput[],
    params?: PostBackupRestoreUsersParams, options?: RequestInit): Promise<postBackupRestoreUsersResponse> => {

  return customFetch<postBackupRestoreUsersResponse>(getPostBackupRestoreUsersUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(mT4UserRestoreInput)
  }
);}


export type postBackupRestoreOrdersResponse200 = {
  data: MT4TradeRestoreResultListApiResponse
  status: 200
}

export type postBackupRestoreOrdersResponseSuccess = (postBackupRestoreOrdersResponse200) & {
  headers: Headers;
};
;

export type postBackupRestoreOrdersResponse = (postBackupRestoreOrdersResponseSuccess)

export const getPostBackupRestoreOrdersUrl = (tradePlatform: string,
    params?: PostBackupRestoreOrdersParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/BackupRestoreOrders?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/BackupRestoreOrders`
}

/**
 * Restore order records into the live MT4 database. Destructive — requires `?confirm=true`.
 *
 * Manager (live) call to the wrapper's
 * `BackupRestoreOrders(TradeRecord[] trades)`. Returns
 * `MT4TradeRestoreResult[]` — one entry per input trade, with
 * `Order` (ticket) and `Res` (0 = error, 1 = restored).
 * Position in the response array matches position in the request.
 * <br>
 * Idempotency-Key header is <b>strongly recommended</b>. Batch cap:
 * 10000 records per call.
 * @summary Restore orders from backup
 */
export const postBackupRestoreOrders = async (tradePlatform: string,
    mT4TradeRestoreInput?: MT4TradeRestoreInput[],
    params?: PostBackupRestoreOrdersParams, options?: RequestInit): Promise<postBackupRestoreOrdersResponse> => {

  return customFetch<postBackupRestoreOrdersResponse>(getPostBackupRestoreOrdersUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST',
    headers: { 'Content-Type': 'application/json-patch+json', ...options?.headers },
    body: JSON.stringify(mT4TradeRestoreInput)
  }
);}


