/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */

/**
 * Request body for the v2 `NotificationsSend` admin endpoint —
 * pushes a single message to one or more MT4 clients identified by
 * account login. Maps onto the wrapper's
 * `NotificationsSend2(int[] logins, string message)`.
 */
export interface MT4NotificationsSendRequest {
  /**
     * Account logins to deliver the notification to (server-side fan-out
     * — the wrapper sends one notification per recipient in a single
     * Manager-API call).
     * @nullable
     */
  logins?: number[] | null;
  /**
     * Notification text. Single-line; the MT4 mobile client displays it
     * as the push body. Controller enforces a length cap to keep the
     * payload tractable on the unmanaged side.
     * @nullable
     */
  message?: string | null;
}
