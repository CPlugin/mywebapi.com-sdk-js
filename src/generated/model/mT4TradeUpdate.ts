/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */

/**
 * Type 1 mutator input for the admin direct-edit endpoint
 * `AdmTradeRecordModify`. Only the fields a back-office tool would
 * legitimately need to adjust are exposed; everything else (order id,
 * login, symbol, volume, open/close times, gateway internals, conversion
 * rates, API data blobs) is preserved from the server-side read.
 *
 * <br>
 * For typical stop-loss / take-profit edits prefer
 * `POST TradeTransaction` with `tradeTransactionType=ModifyTrade`
 * — that goes through the wrapper's audited path. This endpoint is the
 * low-level admin override for back-office corrections.
 */
export interface MT4TradeUpdate {
  /** Order ticket to edit (path parameter is the source of truth) */
  order?: number;
  /** New stop-loss price (0 = remove SL) */
  sl?: number;
  /** New take-profit price (0 = remove TP) */
  tp?: number;
  /** Magic number / EA tag */
  magic?: number;
  /**
     * Comment (broker-visible)
     * @nullable
     */
  comment?: string | null;
  /** Manual commission override */
  commission?: number;
  /** Manual agent-commission override */
  commissionAgent?: number;
  /** Swap / storage override */
  storage?: number;
  /** Profit override (back-office correction only) */
  profit?: number;
  /** Taxes override */
  taxes?: number;
}
