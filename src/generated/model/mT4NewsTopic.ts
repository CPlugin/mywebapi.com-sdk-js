/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */

/**
 * v2 DTO describing a single news topic header as held in the wrapper's
 * pumping cache. Curated subset of the wrapper's NewsTopic: enough to render
 * a list / browse view of broker-distributed news (Key for follow-up
 * NewsBodyGet / NewsBodyRequest, Time, Topic, Category, Keywords, Priority,
 * LangId). The wrapper's `Body` property is intentionally excluded —
 * it is x86-only at the unmanaged layer (the MT4 ManagerAPI lays out the
 * body pointer as a 32-bit field and the wrapper throws
 * System.PlatformNotSupportedException on x64) and is fetched
 * separately via `NewsBodyGet(key)`.
 */
export interface MT4NewsTopic {
  /**
     * News key — the identifier used by `NewsBodyGet` and
     * `NewsBodyRequest` to fetch the body for this topic.
     */
  key?: number;
  /** Published time of the news topic (UTC). */
  time?: string;
  /**
     * News headline / subject (max 256 chars at the wrapper layer).
     * @nullable
     */
  topic?: string | null;
  /**
     * News category. Slash-separated path on the wrapper side (e.g.
     * "Markets\Asian Markets News") used by the MT4 client terminal to
     * build a tree view. Max 64 chars at the wrapper layer.
     * @nullable
     */
  category?: string | null;
  /**
     * Comma-separated keyword list (max 256 chars). Used by quote-feed
     * brokers as a symbol filter — for example `"!EURUSD, EUR*"`
     * selects all EUR pairs except EURUSD.
     * @nullable
     */
  keywords?: string | null;
  /** News priority: 0 = general, 1 = high. */
  priority?: number;
  /**
     * Windows LCID language id. 0 means unspecified; otherwise the low
     * 16 bits of a Windows LCID (the language portion).
     */
  langId?: number;
}
