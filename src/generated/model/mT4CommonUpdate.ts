/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */

/**
 * v2 Type 1 mutator DTO for MT4 server-wide common settings. Curated
 * subset of the wrapper's `ConCommon` struct — exposes the fields
 * most likely to need adjustment from a SaaS surface while leaving
 * runtime counters, derived state, and the wrapper's internal arrays
 * to the secret-preservation overlay on the controller side.
 */
export interface MT4CommonUpdate {
  /**
     * Trade server display name.
     * @nullable
     */
  name?: string | null;
  /**
     * Broker / company display name.
     * @nullable
     */
  owner?: string | null;
  /** Server time zone offset from UTC, in hours. */
  timeZone?: number;
  /** DST correction setting (server-specific integer). */
  daylightCorrection?: number;
  /** Real time-zone (no DST) offset from UTC, in hours. */
  timeZoneReal?: number;
  /**
     * NTP server hostname for clock sync.
     * @nullable
     */
  timeSync?: string | null;
  /** Minimum acceptable client build number. */
  minClient?: number;
  /** Minimum acceptable Manager API build number. */
  minApi?: number;
  /** How long (days) to keep mailbox messages. */
  keepEmails?: number;
  /** How long (days) to keep tick history. */
  keepTicks?: number;
  /** Anti-flood threshold (requests per second). */
  antiFlood?: number;
  /** Flood-control behaviour mode. */
  floodControl?: number;
}
