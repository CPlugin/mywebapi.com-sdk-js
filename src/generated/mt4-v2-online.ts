/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  GetOnlineGetParams,
  GetOnlineRequestParams,
  MT4OnlineListApiResponse
} from './model';

import { customFetch } from '../mutator';


export type getOnlineGetResponse200 = {
  data: MT4OnlineListApiResponse
  status: 200
}

export type getOnlineGetResponseSuccess = (getOnlineGetResponse200) & {
  headers: Headers;
};
;

export type getOnlineGetResponse = (getOnlineGetResponseSuccess)

export const getGetOnlineGetUrl = (tradePlatform: string,
    params?: GetOnlineGetParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/OnlineGet?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/OnlineGet`
}

/**
 * Pump-cached snapshot of currently online users, paginated by login.
 *
 * Pump-cached read — instant local lookup, no MT4 round-trip. Sort
 * order is the login ascending. `?limit=` caps the page size;
 * without it the full set returns in one page. When the pump cache
 * is not yet warmed (fresh connection) the response is a NoConnect
 * error with a retry hint, not an empty list — empty list means
 * "warmed but currently no online sessions".
 * @summary List online users (cached)
 */
export const getOnlineGet = async (tradePlatform: string,
    params?: GetOnlineGetParams, options?: RequestInit): Promise<getOnlineGetResponse> => {

  return customFetch<getOnlineGetResponse>(getGetOnlineGetUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getOnlineRequestResponse200 = {
  data: MT4OnlineListApiResponse
  status: 200
}

export type getOnlineRequestResponseSuccess = (getOnlineRequestResponse200) & {
  headers: Headers;
};
;

export type getOnlineRequestResponse = (getOnlineRequestResponseSuccess)

export const getGetOnlineRequestUrl = (tradePlatform: string,
    params?: GetOnlineRequestParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/OnlineRequest?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/OnlineRequest`
}

/**
 * Live snapshot of every currently-online session, fetched from the MT4 server (Manager round-trip — not pump cache).
 *
 * Manager (live) counterpart to `OnlineGet`. Useful when the pump
 * cache hasn't warmed yet, when staleness is unacceptable, or as a
 * reconciliation pass against the pump snapshot. Heavier than
 * `OnlineGet`: every call hits the MT4 server. Paged the same way
 * for caller symmetry — cursor is the trailing login (ascending).
 * @summary List online sessions (live)
 */
export const getOnlineRequest = async (tradePlatform: string,
    params?: GetOnlineRequestParams, options?: RequestInit): Promise<getOnlineRequestResponse> => {

  return customFetch<getOnlineRequestResponse>(getGetOnlineRequestUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


