/**
 * Generated by orval v8.19.0 🍺
 * Do not edit manually.
 * JSON-Pure WebAPI for MT4/MT5
 * It uses native MetaQuote ManagerAPI for both MT4 and MT5 connection. Plus, it keep pumping connection active, so all *Get calls will run much faster, than *Request.<br/>Here partially used the idea of JSON-Pure API, only HTTP code 200 is acceptable. Other codes signal about errors on server side, which are out of control of client, like wrong parameters, platform unavailability, etc.
 * OpenAPI spec version: 2.0
 */
import type {
  BooleanApiResponse,
  GetDailyReportsRequestExParams,
  GetDailyReportsRequestParams,
  GetPerformanceRequestParams,
  GetReportsRequestParams,
  Int32MT4DailyReportListDictionaryApiResponse,
  MT4DailyReportListApiResponse,
  MT4PerformanceListApiResponse,
  MT4TradeListApiResponse,
  PostDailySyncStartParams
} from './model';

import { customFetch } from '../mutator';


export type getPerformanceRequestResponse200 = {
  data: MT4PerformanceListApiResponse
  status: 200
}

export type getPerformanceRequestResponseSuccess = (getPerformanceRequestResponse200) & {
  headers: Headers;
};
;

export type getPerformanceRequestResponse = (getPerformanceRequestResponseSuccess)

export const getGetPerformanceRequestUrl = (tradePlatform: string,
    params?: GetPerformanceRequestParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/PerformanceRequest?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/PerformanceRequest`
}

/**
 * Time series of MT4 server resource snapshots — CPU, memory, network, sockets, connected-users — captured at server-defined cadence.
 *
 * Manager-live read (round-trip to MT4 server). The MT4 server records
 * these snapshots periodically (typically every five minutes, broker-
 * configurable). Pass from as the earliest timestamp
 * to include; the server returns every snapshot at or after that point
 * up to the present, in ascending `Ctm` order. Useful for capacity
 * dashboards, oncall incident timelines, and load investigations. Pump
 * cache is NOT consulted — data reflects the authoritative server log.
 * Returns an empty list (Ok envelope, not an error) when the window
 * contains no snapshots.
 * @summary Get server performance series
 */
export const getPerformanceRequest = async (tradePlatform: string,
    params?: GetPerformanceRequestParams, options?: RequestInit): Promise<getPerformanceRequestResponse> => {

  return customFetch<getPerformanceRequestResponse>(getGetPerformanceRequestUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getReportsRequestResponse200 = {
  data: MT4TradeListApiResponse
  status: 200
}

export type getReportsRequestResponseSuccess = (getReportsRequestResponse200) & {
  headers: Headers;
};
;

export type getReportsRequestResponse = (getReportsRequestResponseSuccess)

export const getGetReportsRequestUrl = (tradePlatform: string,
    params?: GetReportsRequestParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/ReportsRequest?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/ReportsRequest`
}

/**
 * Closed-trade reports for a batch of account logins within a time window — the data set that drives broker financial reporting (PnL, commissions, taxes) and customer trade-history exports.
 *
 * Manager-live read (round-trip to MT4 server). Pass logins as repeated
 * query parameters: `?logins=1001&logins=1002&logins=1003`.
 * Server-side billing counts this as one Manager request regardless of
 * batch size — prefer one batched call over a per-login loop.
 *
 * The wrapper returns a dictionary keyed by order ticket; the v2 envelope
 * flattens it to a list. Missing logins are silently omitted (no error
 * envelope). The optional name parameter selects a
 * server-defined report template — leave it null/empty to use the
 * default `"RTL_report"` template (closed trades within the window).
 *
 * Pump cache is NOT consulted — data reflects authoritative server
 * history. Note: the wrapper comment warns that asking for a window
 * where the manager account lacks the `Reports` permission may
 * cause MT4 to drop the manager connection; this endpoint guards that
 * indirectly via the API-side `ResourceAccess` check, but a broker
 * that mis-configured the underlying manager rights can still observe
 * transient connection bounces.
 * @summary Get closed-trade reports
 */
export const getReportsRequest = async (tradePlatform: string,
    params?: GetReportsRequestParams, options?: RequestInit): Promise<getReportsRequestResponse> => {

  return customFetch<getReportsRequestResponse>(getGetReportsRequestUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getDailyReportsRequestResponse200 = {
  data: MT4DailyReportListApiResponse
  status: 200
}

export type getDailyReportsRequestResponseSuccess = (getDailyReportsRequestResponse200) & {
  headers: Headers;
};
;

export type getDailyReportsRequestResponse = (getDailyReportsRequestResponseSuccess)

export const getGetDailyReportsRequestUrl = (tradePlatform: string,
    params?: GetDailyReportsRequestParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/DailyReportsRequest?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/DailyReportsRequest`
}

/**
 * End-of-day balance/equity/PnL snapshots for a batch of account logins within a date window — the broker daily-report data set, flat shape (one row per (login, day) pair).
 *
 * Manager-live read (round-trip to MT4 server). Pass logins as repeated
 * query parameters: `?logins=1001&logins=1002&logins=1003`.
 * Server-side billing counts this as one Manager request regardless of
 * batch size — prefer one batched call over a per-login loop.
 *
 * Each row carries its own `Login` field, so the flat shape is
 * joinable on the client side. The MT4 server returns dates in its
 * local time zone, **not** UTC — clients should treat `Ctm` as
 * "broker day boundary" and convert as appropriate.
 *
 * Note: the wrapper warns that asking for a window where the manager
 * account lacks the `Automatic server reports` permission may
 * cause MT4 to drop the manager connection. The API-side
 * `ResourceAccess` check is an indirect guard; a broker that
 * mis-configured the underlying manager rights can still observe
 * transient connection bounces.
 * @summary Get daily reports
 */
export const getDailyReportsRequest = async (tradePlatform: string,
    params?: GetDailyReportsRequestParams, options?: RequestInit): Promise<getDailyReportsRequestResponse> => {

  return customFetch<getDailyReportsRequestResponse>(getGetDailyReportsRequestUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type getDailyReportsRequestExResponse200 = {
  data: Int32MT4DailyReportListDictionaryApiResponse
  status: 200
}

export type getDailyReportsRequestExResponseSuccess = (getDailyReportsRequestExResponse200) & {
  headers: Headers;
};
;

export type getDailyReportsRequestExResponse = (getDailyReportsRequestExResponseSuccess)

export const getGetDailyReportsRequestExUrl = (tradePlatform: string,
    params?: GetDailyReportsRequestExParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/DailyReportsRequestEx?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/DailyReportsRequestEx`
}

/**
 * Same data set as `DailyReportsRequest`, but server-side grouped by login. Convenience shape for clients that pivot the data per-account (per-day rollups, account dashboards).
 *
 * Manager-live read — single round-trip to MT4 server, identical billing
 * cost to `DailyReportsRequest`. The wrapper returns a sorted-list
 * of sorted-lists (by login, then by date); the v2 envelope flattens
 * the inner list to a chronologically-ordered `MT4DailyReport`
 * array, leaving the outer keying by login.
 *
 * JSON shape: `{ "817542": [ ... ], "1001": [ ... ] }` — JSON
 * object keys are strings, so int logins are stringified. Clients
 * should parse keys back to int if needed.
 * @summary Get daily reports (grouped)
 */
export const getDailyReportsRequestEx = async (tradePlatform: string,
    params?: GetDailyReportsRequestExParams, options?: RequestInit): Promise<getDailyReportsRequestExResponse> => {

  return customFetch<getDailyReportsRequestExResponse>(getGetDailyReportsRequestExUrl(tradePlatform,params),
  {
    ...options,
    method: 'GET'


  }
);}


export type postDailySyncStartResponse200 = {
  data: BooleanApiResponse
  status: 200
}

export type postDailySyncStartResponseSuccess = (postDailySyncStartResponse200) & {
  headers: Headers;
};
;

export type postDailySyncStartResponse = (postDailySyncStartResponseSuccess)

export const getPostDailySyncStartUrl = (tradePlatform: string,
    params?: PostDailySyncStartParams,) => {
  const normalizedParams = new URLSearchParams();

  Object.entries(params || {}).forEach(([key, value]) => {

    if (value !== undefined) {
      normalizedParams.append(key, value === null ? 'null' : String(value))
    }
  });

  const stringifiedParams = normalizedParams.toString();

  return stringifiedParams.length > 0 ? `/api/v2/MT4/${tradePlatform}/DailySyncStart?${stringifiedParams}` : `/api/v2/MT4/${tradePlatform}/DailySyncStart`
}

/**
 * Opens a server-side incremental sync session for daily reports modified at or after timestamp. Follow up with `DailySyncRead` to retrieve the snapshot.
 *
 * Manager-live POST (modifies server-side session state). The two-call
 * cycle `DailySyncStart → DailySyncRead` is the broker pattern for
 * pulling only-changed-since-last-poll daily reports; pass
 * `timestamp=0` to request all records.
 *
 * `timestamp` is a Unix epoch second (int32) in MT4 server-local
 * time, not UTC. Wrapper marshals it directly to `__time32_t` —
 * pre-1970 / post-2038 values are out of range.
 *
 * Returns a bare success envelope (no payload); the actual data comes
 * from a subsequent `DailySyncRead` call.
 * @summary Start daily-report sync
 */
export const postDailySyncStart = async (tradePlatform: string,
    params?: PostDailySyncStartParams, options?: RequestInit): Promise<postDailySyncStartResponse> => {

  return customFetch<postDailySyncStartResponse>(getPostDailySyncStartUrl(tradePlatform,params),
  {
    ...options,
    method: 'POST'


  }
);}


export type postDailySyncReadResponse200 = {
  data: MT4DailyReportListApiResponse
  status: 200
}

export type postDailySyncReadResponseSuccess = (postDailySyncReadResponse200) & {
  headers: Headers;
};
;

export type postDailySyncReadResponse = (postDailySyncReadResponseSuccess)

export const getPostDailySyncReadUrl = (tradePlatform: string,) => {




  return `/api/v2/MT4/${tradePlatform}/DailySyncRead`
}

/**
 * Drains the daily-report snapshot opened by the most recent `DailySyncStart` call. Returns every record reserved by the server in that sync session.
 *
 * Manager-live POST (consumes server-side session state — the snapshot
 * is dropped after a successful read). Empty payload (`[]`) is a
 * valid response when the snapshot held no records; this is NOT an
 * error.
 *
 * Call `DailySyncStart` first; calling `DailySyncRead` without
 * a prior `DailySyncStart` may return an empty list or a non-Ok
 * `managerAPICode` depending on server build.
 * @summary Read daily-report sync
 */
export const postDailySyncRead = async (tradePlatform: string, options?: RequestInit): Promise<postDailySyncReadResponse> => {

  return customFetch<postDailySyncReadResponse>(getPostDailySyncReadUrl(tradePlatform),
  {
    ...options,
    method: 'POST'


  }
);}


